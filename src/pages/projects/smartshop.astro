---
import Layout from "../../layouts/Layout.astro";
import listCreation from "../../assets/images/deep-dives/smartshop/list-creation.png";
import route from "../../assets/images/deep-dives/smartshop/route.png";
import share from "../../assets/images/deep-dives/smartshop/share.png";
import { Image } from "astro:assets";
---

<Layout title="Smart Shopping Assistant | Sam Popham" noParticles>
  <section class="max-w-6xl mx-auto px-6 py-12">
    <h1 class="text-4xl font-bold mb-8">
      Smart Shopping Assistant — Deep Dive
    </h1>

    <p class="text-gray-600 dark:text-gray-400 mb-6">
      Cross-platform assistant to create shareable shopping lists and generate
      efficient in-store routes.
    </p>

    <ul class="divide-y divide-gray-200 dark:divide-gray-700">
      <!-- 1: Shopping list UI (image left) -->
      <li class="flex flex-col md:flex-row items-center gap-8 py-12">
        <figure class="md:w-1/2 w-full">
          <!--
            Image guidance:
            App screenshot of shopping-list UI (category grouping visible):
            - list with item categories/aisles, checkboxes, and a small map overlay showing the in-store route,
            - bright, mobile-friendly UI with legible typography.
          -->
          <Image
            src={listCreation}
            alt="Shopping list UI"
            class="w-full rounded-lg shadow-lg"
          />
        </figure>

        <article class="md:w-1/2 w-full">
          <h2 class="text-2xl font-semibold mb-3">List UI & Categories</h2>
          <p class="text-gray-600 dark:text-gray-400">
            Organised lists reduce friction — group by aisle to skip the zigzags
            and shop like a pro.
          </p>
        </article>
      </li>

      <!-- 2: Route overlay (image right) -->
      <li
        class="flex flex-col md:flex-row md:flex-row-reverse items-center gap-8 py-12"
      >
        <figure class="md:w-1/2 w-full">
          <!--
            Image guidance:
            Route overlay on store map:
            - map with suggested in-store route and stop order to visualise optimisation output,
            - clear path lines and numbered stops.
          -->
          <Image
            src={route}
            alt="Route overlay"
            class="w-full rounded-lg shadow-lg"
          />
        </figure>

        <article class="md:w-1/2 w-full">
          <h2 class="text-2xl font-semibold mb-3">In-Store Route</h2>
          <p class="text-gray-600 dark:text-gray-400">
            A good route saves steps and time — the app models typical store
            layouts to plan the most efficient path.
          </p>
        </article>
      </li>

      <!-- 3: Share / sync (image left) -->
      <li class="flex flex-col md:flex-row items-center gap-8 py-12">
        <figure class="md:w-1/2 w-full">
          <!--
            Image guidance:
            Share / sync screen:
            - share modal or sync status demonstrating collaborative list features,
            - show collaborators and permission UI.
          -->
          <Image
            src={share}
            alt="Share and sync UI"
            class="w-full rounded-lg shadow-lg"
          />
        </figure>

        <article class="md:w-1/2 w-full">
          <h2 class="text-2xl font-semibold mb-3">Sharing & Sync</h2>
          <p class="text-gray-600 dark:text-gray-400">
            Share lists with family and sync changes live — teamwork makes the
            shopping dream work.
          </p>
        </article>
      </li>
    </ul>
  </section>
</Layout>
>
